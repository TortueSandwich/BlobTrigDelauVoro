// Model.scala
import scalafx.beans.property.{ObjectProperty, ReadOnlyBooleanProperty}
import scalafx.collections.ObservableHashSet
import scala.collection.mutable
import scalafx.beans.binding.Bindings

class Model {

  // var quadedge: ObjectProperty[QuadEdge] = ObjectProperty(
  //   null
  //   // Delaunay.TriangulateDelaunay(points.get.toList)
  // )

  // val points: [Iterable[FinitePoint]] = ObjectProperty(
  //   // List.empty
  //   quadedge
  // )
  // val lines: ObjectProperty[Iterable[(FinitePoint, FinitePoint)]] = ObjectProperty(Seq.empty)
  // val selection: ObservableHashSet[FinitePoint] = new ObservableHashSet[FinitePoint]()
  
  

}
